cmake_minimum_required(VERSION 3.10)

#string(REGEX REPLACE "(.*)/(.*)" "\\0" PROJECT_INIT_PATH ${PROJECT_SOURCE_DIR})
#message("上层目录=" ${PROJECT_INIT_PATH})


project(GASR)
add_subdirectory(position)